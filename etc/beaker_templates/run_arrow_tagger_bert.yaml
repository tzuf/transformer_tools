description: Train BERT tagger model on polarity data
tasks:
- spec:
    image: im_x4f3jgolbvzv
    resultPath: /output
    args: [python, -m, transformer_tools, Tagger, --output_dir, /output, --data_dir, /inputs, --num_train_epochs, "1", --learning_rate, "0.00001", --train_batch_size, "16", --seed, "42", --cloud, --early_stopping, --label_list,"B-up;B-down;B-=",--dev_eval,--print_output]
    env:
      DATASET : "polarity_2"
      LR : "0.00001"
      MAX_ITERATIONS : 3
      MODEL : "BERT"
      BATCH_SIZE : 16
    datasetMounts:
    - datasetId: ds_ez2215eexjt2
      containerPath: /inputs
    requirements:
      gpuCount: 1
  cluster: us_wvnghctl47k0/01E5TXXY2BN0XNQHF7QHSDJ5YR